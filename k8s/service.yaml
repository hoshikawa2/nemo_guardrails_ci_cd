apiVersion: v1
kind: Service
metadata:
  name: guardrails-service
  namespace: guardrails
spec:
  type: LoadBalancer
  selector:
    app: guardrails-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
